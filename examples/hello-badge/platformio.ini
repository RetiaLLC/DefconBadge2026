; hello-badge: first-steps test program for the 2024 DEF CON badge.
; Blinks the debug LED, prints button presses on the USB console, and
; beeps the buzzer with a different tone per button.

[env:defcon-badge]
platform = espressif32
board = defcon-badge
framework = espidf
monitor_speed = 115200
